name: Heartbeat Watcher

on:
  workflow_dispatch:
  push:
    paths:
      - "HEARTBEAT.md"

permissions:
  contents: read

jobs:
  notify:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Extract last heartbeat entry
        id: heartbeat
        shell: bash
        run: |
          set -euo pipefail

          if [ ! -f HEARTBEAT.md ]; then
            echo "HEARTBEAT.md not found" >&2
            exit 1
          fi

          # last non-empty line (tail -n 1 returns empty if file ends with blank line)
          LAST_LINE="$(grep -v '^[[:space:]]*$' HEARTBEAT.md | tail -n 1 || true)"

          if [ -z "${LAST_LINE:-}" ]; then
            echo "HEARTBEAT.md has no non-empty lines" >&2
            exit 1
          fi

          echo "last_line=$LAST_LINE" >> "$GITHUB_OUTPUT"

      - name: Send Discord notification
        will@ubs24:~/openclaw/apps/wjbetech-dashboard/.github/workflows$ cat c
i.yml
name: CI

on:
  pull_request:
    branches: [ main ]
  push:
    branches: [ main ]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
      - run: npm ci
      - run: npm run lint --if-present
      - name: Lint backend if present
        if: ${{ exists('backend/package.json') }}
        run: |
          cd backend
          npm ci
          npm run lint --if-present

  typecheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
      - run: npm ci
      - run: npm run typecheck --if-present

  unit:
    runs-on: ubuntu-latest
    needs: [lint, typecheck]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
      - run: npm ci
      - run: npm run build --if-present
      - run: npx vitest run

  e2e:
    runs-on: ubuntu-latest
    needs: unit
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
      - run: npm ci
      - run: npx playwright install --with-deps
      - run: npx playwright test
will@ubs24:~/openclaw/apps/wjbetech-dashboard/.github/workflows$ cat h
eartbeat-watcher.yml 
name: Heartbeat Watcher

on:
  workflow_dispatch:
  push:
    paths:
      - "HEARTBEAT.md"

permissions:
  contents: read

jobs:
  notify:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Extract last heartbeat entry
        id: heartbeat
        shell: bash
        run: |
          set -euo pipefail

          if [ ! -f HEARTBEAT.md ]; then
            echo "HEARTBEAT.md not found" >&2
            exit 1
          fi

          # last non-empty line (tail -n 1 returns empty if file ends with blank line)
          LAST_LINE="$(grep -v '^[[:space:]]*$' HEARTBEAT.md | tail -n 1 || true)"

          if [ -z "${LAST_LINE:-}" ]; then
            echo "HEARTBEAT.md has no non-empty lines" >&2
            exit 1
          fi

          echo "last_line=$LAST_LINE" >> "$GITHUB_OUTPUT"

      - name: Send Discord notification
        if: ${{ secrets.DISCORD_WEBHOOK_URL != '' }}
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        shell: bash
        run: |
          set -euo pipefail

          payload=$(printf '{"content":"🛠 OpenClaw Update:\\n`%s`"}' "${{ steps.heartbeat.outputs.last_line }}")

          curl -fsS -X POST \
            -H "Content-Type: application/json" \
            -d "$payload" \
            "$DISCORD_WEBHOOK_URL"

      - name: Skip notification (no webhook configured)
        if: ${{ secrets.DISCORD_WEBHOOK_URL == '' }}
        run: echo "DISCORD_WEBHOOK_URL not configured; skipping notification."
