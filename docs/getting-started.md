# Getting Started

Prerequisites
- Node 18+ (recommended)
- npm or pnpm

Local dev
1. Clone the repo
2. Install dependencies: `npm install`
3. Run dev server: `npm run dev`

Build
- `npm run build`

Notes
- The app uses Vite + React + Tailwind.
