# Deployment

This project is deployed on Vercel. Key notes:
- Production branch: main
- Build command: `npm run build`
- Output directory: `dist`

If you see a 404 on Vercel, ensure the project root is correct and the build produces `dist/index.html`.
